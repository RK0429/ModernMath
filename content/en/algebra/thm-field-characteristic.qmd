---
id: thm-field-characteristic
requires:
- def-field
- def-prime
- def-ring
status: complete
title: Field Characteristic
translations:
  en: ../en/algebra/thm-field-characteristic.html
  ja: ../ja/algebra/thm-field-characteristic.html
type: Theorem
---

# Field Characteristic {#thm-field-characteristic}

Every [Field](def-field.qmd) has characteristic either 0 or a [Prime Number](../number-theory/def-prime.qmd) number.

## Definitions

The **characteristic** of a field $F$, denoted $\text{char}(F)$, is the smallest positive integer $n$ such that:
$$\underbrace{1 + 1 + ... + 1}_{n \text{ times}} = 0$$

If no such $n$ exists, we say $\text{char}(F) = 0$.

## Statement

For any field $F$:
1. If $\text{char}(F) \neq 0$, then $\text{char}(F) = p$ for some prime $p$
2. If $\text{char}(F) = 0$, then $F$ contains a copy of $\mathbb{Q}$
3. If $\text{char}(F) = p$, then $F$ contains a copy of $\mathbb{F}_p$

## Proof Sketch

Suppose $\text{char}(F) = n > 0$. If $n = ab$ with $1 < a, b < n$, then:
$$(a \cdot 1)(b \cdot 1) = (ab) \cdot 1 = n \cdot 1 = 0$$

Since fields have no [zero divisors](def-zero-divisor.qmd), either $a \cdot 1 = 0$ or $b \cdot 1 = 0$, contradicting the minimality of $n$. Therefore $n$ must be prime.

## Examples

- $\text{char}(\mathbb{Q}) = 0$, $\text{char}(\mathbb{R}) = 0$, $\text{char}(\mathbb{C}) = 0$
- $\text{char}(\mathbb{F}_p) = p$ for any prime $p$
- $\text{char}(GF(p^n)) = p$ for any prime power $p^n$

## Consequences

- In characteristic $p$: $(a + b)^p = a^p + b^p$ (Freshman's Dream)
- The Frobenius map $x \mapsto x^p$ is a field [automorphism](def-automorphism.qmd) in finite fields of characteristic $p$

## Interactive Visualization

Explore the local knowledge graph neighborhood interactively:

::: {.graph-viz data-id="thm-field-characteristic" data-width="700" data-height="500"}
:::

You can:
- **Drag** nodes to rearrange the layout
- **Zoom** in/out with your mouse wheel
- **Hover** over nodes to see details
- View the [full interactive version](../../output/interactive/en/thm-field-characteristic.html){target="_blank"}
